@page
@using Final_new.Services
@inject DemoDataService DemoData

@{
    string? emailId = Request.Query["EmailID"];
    if (!string.IsNullOrEmpty(emailId))
    {
        DemoData.DeleteEmail(emailId);
    }
    Response.Redirect("/SentEmail");
}
